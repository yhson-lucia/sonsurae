-- 손수레 시드 데이터
--
-- Phase 7 (데이터 마이그레이션) 에서 채워진다.
-- 카테고리 20개 + 작성자 프로필 + (마이그레이션 스크립트가 글/이미지 적재).
--
-- 지금은 의도적으로 비워둔다 — 다른 phase 작업에 카테고리 시드가 필요해지면
-- 이 파일이 아니라 mock 데이터(`src/lib/mock-posts.ts`)에서 다룬다.

-- intentionally empty
